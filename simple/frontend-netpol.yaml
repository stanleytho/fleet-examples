apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-allow-all
spec:
  egress:
  - {}
  ingress:
  - {}
  podSelector:
    matchLabels:
      app: guestbook
  policyTypes:
    - Ingress
    - Egress
